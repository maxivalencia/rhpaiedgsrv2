<form method="post" action="{{ path('decisions_affectations_delete', {'id': decisions_affectation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decisions_affectation.id) }}">
    <button class="btn">Delete</button>
</form>
