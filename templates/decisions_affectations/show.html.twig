{% extends 'base.html.twig' %}

{% block title %}Décisions Affectations{% endblock %}

{% block body %}
    <h1>Décisions Affectations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decisions_affectation.id }}</td>
            </tr>
            <tr>
                <th>Reférence de la décision</th>
                <td>{{ decisions_affectation.referenceDecision }}</td>
            </tr>
            <tr>
                <th>Date de la décision</th>
                <td>{{ decisions_affectation.dateDecision ? decisions_affectation.dateDecision|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>Genre de décision</th>
                <td>{{ decisions_affectation.genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('decisions_affectations_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('decisions_affectations_edit', {'id': decisions_affectation.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('decisions_affectations/_delete_form.html.twig') }}
{% endblock %}
