<form method="post" action="{{ path('decisions_promotions_delete', {'id': decisions_promotion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decisions_promotion.id) }}">
    <button class="btn">Delete</button>
</form>
