{% extends 'base.html.twig' %}

{% block title %}Décisions Radiations Controles{% endblock %}

{% block body %}
    <h1>Décisions Radiations Controles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decisions_radiations_controle.id }}</td>
            </tr>
            <tr>
                <th>Reférence de la décision</th>
                <td>{{ decisions_radiations_controle.referenceDecision }}</td>
            </tr>
            <tr>
                <th>Date de reférence</th>
                <td>{{ decisions_radiations_controle.dateReference ? decisions_radiations_controle.dateReference|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>Reférence dans le journal officiel</th>
                <td>{{ decisions_radiations_controle.referenceJournalOfficiel }}</td>
            </tr>
            <tr>
                <th>Date dans le journal officiel</th>
                <td>{{ decisions_radiations_controle.dateJournalOfficiel ? decisions_radiations_controle.dateJournalOfficiel|date('d/m/Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('decisions_radiations_controles_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('decisions_radiations_controles_edit', {'id': decisions_radiations_controle.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('decisions_radiations_controles/_delete_form.html.twig') }}
{% endblock %}
