{% extends 'base.html.twig' %}

{% block title %}Liste Decorations{% endblock %}

{% block body %}
    <h1>Liste Decorations</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Abbreviation</th>
                <th>Ordre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for decoration in decorations %}
            <tr>
                <td>{{ decoration.id }}</td>
                <td>{{ decoration.libelle }}</td>
                <td>{{ decoration.abbreviation }}</td>
                <td>{{ decoration.ordre }}</td>
                <td>
                    <a href="{{ path('decorations_show', {'id': decoration.id}) }}"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('decorations_edit', {'id': decoration.id}) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('decorations_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
{% endblock %}
