<form method="post" action="{{ path('details_motifs_radiations_controles_delete', {'id': details_motifs_radiations_controle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ details_motifs_radiations_controle.id) }}">
    <button class="btn">Delete</button>
</form>
