{% extends 'base.html.twig' %}

{% block title %}Liste Détails Motifs Radiations Controles{% endblock %}

{% block body %}
    <h1>Liste Détails Motifs Radiations Controles</h1>

    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Détail motif radiation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for details_motifs_radiations_controle in details_motifs_radiations_controles %}
            <tr>
                {# <td>{{ details_motifs_radiations_controle.id }}</td> #}
                <td>{{ details_motifs_radiations_controle.detailMotifRadiation }}</td>
                <td>
                    <a href="{{ path('details_motifs_radiations_controles_show', {'id': details_motifs_radiations_controle.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('details_motifs_radiations_controles_edit', {'id': details_motifs_radiations_controle.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('details_motifs_radiations_controles_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
{% endblock %}
