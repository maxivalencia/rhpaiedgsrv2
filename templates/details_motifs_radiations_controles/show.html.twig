{% extends 'base.html.twig' %}

{% block title %}Détails Motifs Radiations Contrôles{% endblock %}

{% block body %}
    <h1>Détails Motifs Radiations Contrôles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ details_motifs_radiations_controle.id }}</td>
            </tr>
            <tr>
                <th>Détail du motif de radiation</th>
                <td>{{ details_motifs_radiations_controle.detailMotifRadiation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('details_motifs_radiations_controles_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('details_motifs_radiations_controles_edit', {'id': details_motifs_radiations_controle.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('details_motifs_radiations_controles/_delete_form.html.twig') }}
{% endblock %}
