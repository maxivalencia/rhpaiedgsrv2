{% extends 'base.html.twig' %}

{% block title %}Liste Diplomes{% endblock %}

{% block body %}
    <h1>Liste Diplomes</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Abbreviation</th>
                <th>Type</th>
                <th>Est diplome militaire</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for diplome in diplomes %}
            <tr>
                <td>{{ diplome.id }}</td>
                <td>{{ diplome.libelle }}</td>
                <td>{{ diplome.abbreviation }}</td>
                <td>{{ diplome.type }}</td>
                <td>{{ diplome.estDiplomeMilitaire ? 'Oui' : 'Non' }}</td>
                <td>
                    <a href="{{ path('diplomes_show', {'id': diplome.id}) }}"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('diplomes_edit', {'id': diplome.id}) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('diplomes_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
{% endblock %}
