{% extends 'base.html.twig' %}

{% block title %}Districts{% endblock %}

{% block body %}
    <h1>Districts</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ district.id }}</td>
            </tr> #}
            <tr>
                <th>District</th>
                <td>{{ district.district }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('districts_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('districts_edit', {'id': district.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('districts/_delete_form.html.twig') }}
{% endblock %}
