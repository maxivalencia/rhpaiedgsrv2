{% extends 'base.html.twig' %}

{% block title %}DomaineDiplome index{% endblock %}

{% block body %}
    <h1>DomaineDiplome index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for domaine_diplome in domaine_diplomes %}
            <tr>
                <td>{{ domaine_diplome.id }}</td>
                <td>{{ domaine_diplome.libelle }}</td>
                <td>
                    <a href="{{ path('domaine_diplome_show', {'id': domaine_diplome.id}) }}">show</a>
                    <a href="{{ path('domaine_diplome_edit', {'id': domaine_diplome.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('domaine_diplome_new') }}">Create new</a>
{% endblock %}
