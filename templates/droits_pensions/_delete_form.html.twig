<form method="post" action="{{ path('droits_pensions_delete', {'id': droits_pension.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ droits_pension.id) }}">
    <button class="btn">Delete</button>
</form>
