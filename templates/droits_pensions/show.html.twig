{% extends 'base.html.twig' %}

{% block title %}Droits Pensions{% endblock %}

{% block body %}
    <h1>Droits Pensions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ droits_pension.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ droits_pension.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('droits_pensions_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('droits_pensions_edit', {'id': droits_pension.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('droits_pensions/_delete_form.html.twig') }}
{% endblock %}
