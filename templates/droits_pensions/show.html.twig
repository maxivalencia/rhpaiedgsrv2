{% extends 'base.html.twig' %}

{% block title %}DroitsPensions{% endblock %}

{% block body %}
    <h1>DroitsPensions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ droits_pension.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ droits_pension.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('droits_pensions_index') }}">back to list</a>

    <a href="{{ path('droits_pensions_edit', {'id': droits_pension.id}) }}">edit</a>

    {{ include('droits_pensions/_delete_form.html.twig') }}
{% endblock %}
