<form method="post" action="{{ path('fonctions_conjoints_delete', {'id': fonctions_conjoint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fonctions_conjoint.id) }}">
    <button class="btn">Delete</button>
</form>
