{% extends 'base.html.twig' %}

{% block title %}Fonctions Conjoints{% endblock %}

{% block body %}
    <h1>Fonctions Conjoints</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ fonctions_conjoint.id }}</td>
            </tr> #}
            <tr>
                <th>Libelle</th>
                <td>{{ fonctions_conjoint.libelle }}</td>
            </tr>
            <tr>
                <th>Abbréviation</th>
                <td>{{ fonctions_conjoint.abbreviation }}</td>
            </tr>
            <tr>
                <th>Lieu de travail</th>
                <td>{{ fonctions_conjoint.lieuTravail }}</td>
            </tr>
            <tr>
                <th>Employeur</th>
                <td>{{ fonctions_conjoint.employeur }}</td>
            </tr>
            <tr>
                <th>Adresse de l'employeur</th>
                <td>{{ fonctions_conjoint.adresseEmployeur }}</td>
            </tr>
            <tr>
                <th>Est fonctionnaire</th>
                <td>{{ fonctions_conjoint.estFonctionnaire ? 'Oui' : 'Non' }}</td>
            </tr>
            <tr>
                <th>Type de contrat de travail</th>
                <td>{{ fonctions_conjoint.typeContrat }}</td>
            </tr>
            <tr>
                <th>Conjoint</th>
                <td>{{ fonctions_conjoint.conjoint }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fonctions_conjoints_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('fonctions_conjoints_edit', {'id': fonctions_conjoint.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('fonctions_conjoints/_delete_form.html.twig') }}
{% endblock %}
