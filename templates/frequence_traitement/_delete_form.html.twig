<form method="post" action="{{ path('frequence_traitement_delete', {'id': frequence_traitement.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce donnée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frequence_traitement.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</button>
</form>
