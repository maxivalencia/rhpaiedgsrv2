{% extends 'base.html.twig' %}

{% block title %}Grades{% endblock %}

{% block body %}
    <h1>Grades</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ grade.id }}</td>
            </tr> #}
            <tr>
                <th>Grade</th>
                <td>{{ grade.grade }}</td>
            </tr>
            <tr>
                <th>Abbréviation</th>
                <td>{{ grade.abbreviation }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ grade.rang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grades_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('grades_edit', {'id': grade.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('grades/_delete_form.html.twig') }}
{% endblock %}
