{% extends 'base.html.twig' %}

{% block title %}Motif de Réintegration{% endblock %}

{% block body %}
    <h1>Motif de Réintegration</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ motif_reintegration.id }}</td>
            </tr> #}
            <tr>
                <th>Motif de Réintegration</th>
                <td>{{ motif_reintegration.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('motif_reintegration_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>

    <a href="{{ path('motif_reintegration_edit', {'id': motif_reintegration.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('motif_reintegration/_delete_form.html.twig') }}
{% endblock %}
