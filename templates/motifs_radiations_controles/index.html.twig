{% extends 'base.html.twig' %}

{% block title %}MotifsRadiationsControles index{% endblock %}

{% block body %}
    <h1>MotifsRadiationsControles index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for motifs_radiations_controle in motifs_radiations_controles %}
            <tr>
                <td>{{ motifs_radiations_controle.id }}</td>
                <td>{{ motifs_radiations_controle.libelle }}</td>
                <td>
                    <a href="{{ path('motifs_radiations_controles_show', {'id': motifs_radiations_controle.id}) }}">show</a>
                    <a href="{{ path('motifs_radiations_controles_edit', {'id': motifs_radiations_controle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('motifs_radiations_controles_new') }}">Create new</a>
{% endblock %}
