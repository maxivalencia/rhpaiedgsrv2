{% extends 'base.html.twig' %}

{% block title %}MotifsRadiationsControles{% endblock %}

{% block body %}
    <h1>MotifsRadiationsControles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motifs_radiations_controle.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ motifs_radiations_controle.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('motifs_radiations_controles_index') }}">back to list</a>

    <a href="{{ path('motifs_radiations_controles_edit', {'id': motifs_radiations_controle.id}) }}">edit</a>

    {{ include('motifs_radiations_controles/_delete_form.html.twig') }}
{% endblock %}
