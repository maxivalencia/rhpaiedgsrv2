{% extends 'base.html.twig' %}

{% block title %}Motifs Radiations Contrôles{% endblock %}

{% block body %}
    <h1>Motifs Radiations Contrôles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motifs_radiations_controle.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ motifs_radiations_controle.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('motifs_radiations_controles_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('motifs_radiations_controles_edit', {'id': motifs_radiations_controle.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('motifs_radiations_controles/_delete_form.html.twig') }}
{% endblock %}
