{% extends 'base.html.twig' %}

{% block title %}Nature{% endblock %}

{% block body %}
    <h1>Nature</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ nature.id }}</td>
            </tr> #}
            <tr>
                <th>Libelle</th>
                <td>{{ nature.libelle }}</td>
            </tr>
            <tr>
                <th>Abreviation</th>
                <td>{{ nature.abreviation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nature_index') }}">back to list</a>

    <a href="{{ path('nature_edit', {'id': nature.id}) }}">edit</a>

    {{ include('nature/_delete_form.html.twig') }}
{% endblock %}
