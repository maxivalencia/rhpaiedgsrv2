{% extends 'base.html.twig' %}

{% block title %}Nature{% endblock %}

{% block body %}
    <h1>Nature</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ nature.id }}</td>
            </tr> #}
            <tr>
                <th>Libelle</th>
                <td>{{ nature.libelle }}</td>
            </tr>
            <tr>
                <th>Abreviation</th>
                <td>{{ nature.abreviation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nature_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>

    <a href="{{ path('nature_edit', {'id': nature.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('nature/_delete_form.html.twig') }}
{% endblock %}
