{% extends 'base.html.twig' %}

{% block title %}NiveauDiplome index{% endblock %}

{% block body %}
    <h1>NiveauDiplome index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for niveau_diplome in niveau_diplomes %}
            <tr>
                <td>{{ niveau_diplome.id }}</td>
                <td>{{ niveau_diplome.libelle }}</td>
                <td>
                    <a href="{{ path('niveau_diplome_show', {'id': niveau_diplome.id}) }}">show</a>
                    <a href="{{ path('niveau_diplome_edit', {'id': niveau_diplome.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('niveau_diplome_new') }}">Create new</a>
{% endblock %}
