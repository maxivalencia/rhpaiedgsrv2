{% extends 'base.html.twig' %}

{% block title %}Liste Niveau Diplôme{% endblock %}

{% block body %}
    <h1>Liste Niveau Diplôme</h1>
    <form class="mt-2 mt-md-0 d-none d-lg-block search-input" action="{{ path('niveau_diplome_recherche') }}">
        <div class="input-group">
            <span class="input-group-addon d-flex align-items-center"><i class="icon-magnifier icons"></i></span>
            <input type="text" name="search" class="form-control" placeholder="Search...">
        </div>
    </form>
    <a href="{{ path('niveau_diplome_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for niveau_diplome in niveau_diplomes %}
            <tr>
                {# <td>{{ niveau_diplome.id }}</td> #}
                <td>{{ niveau_diplome.libelle }}</td>
                <td>
                    <a href="{{ path('niveau_diplome_show', {'id': niveau_diplome.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('niveau_diplome_edit', {'id': niveau_diplome.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(niveau_diplomes) }}
    </div>

    <a href="{{ path('niveau_diplome_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
{% endblock %}
