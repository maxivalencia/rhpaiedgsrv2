{% extends 'base.html.twig' %}

{% block title %}Edition Note POS{% endblock %}

{% block body %}
    <h1>Edition Note POS</h1>

    {{ include('notes_pos/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('notes_p_o_s_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>

    {{ include('notes_pos/_delete_form.html.twig') }}
{% endblock %}
