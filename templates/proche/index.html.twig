{% extends 'base.html.twig' %}

{% block title %}Liste Proche famille{% endblock %}

{% block body %}
    <h1>Liste Proche famille</h1>
    <a href="{{ path('proche_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
    <table id="user_liste" class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Proche famille</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for proche in proches %}
            <tr>
                {# <td>{{ proche.id }}</td> #}
                <td>{{ proche.libelle }}</td>
                <td>
                    <a href="{{ path('proche_show', {'id': proche.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('proche_edit', {'id': proche.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('proche_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>

    {% block javascripts %}
        <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.12.1/js/jquery.dataTables.js"></script>
        <script>
            $(document).ready( function () {
                $('#user_liste').DataTable();
            } );
        </script>
    {% endblock %}
{% endblock %}
