{% extends 'base.html.twig' %}

{% block title %}Proche famille{% endblock %}

{% block body %}
    <h1>Proche famille</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ proche.id }}</td>
            </tr> #}
            <tr>
                <th>Proche famille</th>
                <td>{{ proche.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proche_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>

    <a href="{{ path('proche_edit', {'id': proche.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('proche/_delete_form.html.twig') }}
{% endblock %}
