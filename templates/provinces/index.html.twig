{% extends 'base.html.twig' %}

{% block title %}Liste Provinces{% endblock %}

{% block body %}
    <h1>Liste Provinces</h1>

    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Province</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for province in provinces %}
            <tr>
                {# <td>{{ province.id }}</td> #}
                <td>{{ province.province }}</td>
                <td>
                    <a href="{{ path('provinces_show', {'id': province.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('provinces_edit', {'id': province.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(provinces) }}
    </div>

    <a href="{{ path('provinces_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
{% endblock %}
