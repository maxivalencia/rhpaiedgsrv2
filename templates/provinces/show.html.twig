{% extends 'base.html.twig' %}

{% block title %}Provinces{% endblock %}

{% block body %}
    <h1>Provinces</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ province.id }}</td>
            </tr> #}
            <tr>
                <th>Province</th>
                <td>{{ province.province }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('provinces_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('provinces_edit', {'id': province.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('provinces/_delete_form.html.twig') }}
{% endblock %}
