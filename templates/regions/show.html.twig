{% extends 'base.html.twig' %}

{% block title %}Régions{% endblock %}

{% block body %}
    <h1>Régions</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ region.id }}</td>
            </tr> #}
            <tr>
                <th>Région</th>
                <td>{{ region.region }}</td>
            </tr>
            <tr>
                <th>Province</th>
                <td>{{ region.province }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('regions_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('regions_edit', {'id': region.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('regions/_delete_form.html.twig') }}
{% endblock %}
