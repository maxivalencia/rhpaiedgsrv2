{% extends 'base.html.twig' %}

{% block title %}Roles{% endblock %}

{% block body %}
    <h1>Roles</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr> #}
            <tr>
                <th>Role</th>
                <td>{{ role.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('roles_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>

    <a href="{{ path('roles_edit', {'id': role.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('roles/_delete_form.html.twig') }}
{% endblock %}
