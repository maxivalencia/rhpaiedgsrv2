<form method="post" action="{{ path('suivie_apres_traitement_delete', {'id': suivie_apres_traitement.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce donnée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivie_apres_traitement.id) }}">
<button class="btn btn-sm bg-gradient-danger m-2"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</button>
</form>
