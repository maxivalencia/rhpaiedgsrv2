{% extends 'base.html.twig' %}

{% block title %}Suivie Après Traitement{% endblock %}

{% block body %}
    <h1>Suivie Après Traitement</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ suivie_apres_traitement.id }}</td>
            </tr> #}
            <tr>
                <th>Suivie Après Traitement</th>
                <td>{{ suivie_apres_traitement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('suivie_apres_traitement_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>

    <a href="{{ path('suivie_apres_traitement_edit', {'id': suivie_apres_traitement.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('suivie_apres_traitement/_delete_form.html.twig') }}
{% endblock %}
