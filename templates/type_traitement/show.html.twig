{% extends 'base.html.twig' %}

{% block title %}Type de Traitement{% endblock %}

{% block body %}
    <h1>Type de Traitement</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ type_traitement.id }}</td>
            </tr> #}
            <tr>
                <th>Type de traitement</th>
                <td>{{ type_traitement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_traitement_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>

    <a href="{{ path('type_traitement_edit', {'id': type_traitement.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('type_traitement/_delete_form.html.twig') }}
{% endblock %}
