<form method="post" action="{{ path('types_communes_delete', {'id': types_commune.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_commune.id) }}">
    <button class="btn">Delete</button>
</form>
