{% extends 'base.html.twig' %}

{% block title %}Types de Communes{% endblock %}

{% block body %}
    <h1>Types de Communes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_commune.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ types_commune.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('types_communes_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('types_communes_edit', {'id': types_commune.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('types_communes/_delete_form.html.twig') }}
{% endblock %}
