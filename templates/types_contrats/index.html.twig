{% extends 'base.html.twig' %}

{% block title %}Liste Types de Contrats{% endblock %}

{% block body %}
    <h1>Liste Types de Contrats</h1>
    <form class="mt-2 mt-md-0 d-none d-lg-block search-input" action="{{ path('types_contrats_recherche') }}">
        <div class="input-group">
            <span class="input-group-addon d-flex align-items-center"><i class="icon-magnifier icons"></i></span>
            <input type="text" name="search" class="form-control" placeholder="Search...">
        </div>
    </form>
    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for types_contrat in types_contrats %}
            <tr>
                {# <td>{{ types_contrat.id }}</td> #}
                <td>{{ types_contrat.type }}</td>
                <td>
                    <a href="{{ path('types_contrats_show', {'id': types_contrat.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('types_contrats_edit', {'id': types_contrat.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(types_contrats) }}
    </div>

    <a href="{{ path('types_contrats_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer</a>
{% endblock %}
